name: 'insurance_mart'
version: '1.0.0'
config-version: 2

profile: 'insurance_mart'

model-paths: ["models"]
test-paths: ["tests"]

models:
  insurance_mart:
    staging:
      +materialized: view
      +schema: staging

    intermediate:
      +materialized: view
      +schema: intermediate

    marts:
      +materialized: table
      +schema: marts